#include "main.h"

/** _is.lower - Checks for ay lower case characters
 * @c: character to be checked
 * Return: 1 if c is lowercase or 0 if not
 */
int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{
		return (1);
	}
}
