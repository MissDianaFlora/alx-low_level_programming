#include "main.h"

/**
 * mul - Multiplies two integers
 * @a: The first integer to multiply
 * @b: the second number to be multiplied with first number
 * Return: the result of the multiplication of x and y
 */
int mul(int a, int b)
{
	return (a * b);
}
